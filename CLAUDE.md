# 경건생활 앱 — 청소년부 & 학부모 (godly-life-family)

## 목적

혜림교회 청소년부 **우리반 학생과 학부모**가 매일 자신의 경건생활을 기록·점검하고,
담임교사에게 기도를 요청하며, 개혁주의 신앙고백 자료를 찾아볼 수 있는 휴대폰용 웹앱.

사용자는 **자기 기록만** 볼 수 있고, 담임교사(관리자)만 반 전체를 조회한다.
데이터는 Google Sheets에 저장하며, 사용자에게는 시트 접근 권한을 주지 않는다.

참고 저장소: `kyuils/godly-life-check` (찬양팀용 선행 앱) — **읽기 전용 참고**. 구조·인증·시트 I/O 패턴을 계승한다.

## 핵심 기능 (요구사항 — 임의 추가·삭제 금지)

1. **날주(맥체인 성경일기) 체크** — 말씀읽음 체크, 와닿은 말씀, 결단, 중보기도 체크
2. **월 달력** — 본인의 날주 기록을 한 달 달력으로 한눈에. 날짜를 누르면 그날 기록 상세
3. **담임교사에게 요청하는 기도** — 본인과 교사만 열람. 응답 여부 체크(본인만).
   월별 목록으로 관리. 응답된 기도는 날짜만 접어서 표시하고 누르면 펼침. 미응답은 계속 펼쳐서 표시
4. **참고자료** — 신도게요서(웨스트민스터 신앙고백서)·소요리문답·대요리문답·예배모범·신조
   (출처: 대한예수교장로회 합동 총회 전자자료실) + 하이델베르크 요리문답(별도 공개 번역본)
5. **구글 계정 로그인 + 자가 등록**
6. **학생/학부모 구분** — 가입 시 본인이 선택, 구글 시트에서 **명부를 시트별로 분리** 관리
7. **DB = Google Drive의 Google Sheets**
8. **보안** — 사용자는 본인 데이터만 조회. 관리자 외 아무도 시트에 접근 불가
9. **GitHub 저장소로 관리**

## 아키텍처

- `web/` — 정적 프론트엔드 (GitHub Pages 배포, PWA). ES modules로 화면별 분리
- `gas/*.gs` — Google Apps Script 웹앱 백엔드 (Sheets 읽기/쓰기 API)
- Google Sheets — 데이터 저장소 (담임교사 계정 소유, 비공유)
- `web/data/*.json` — 참고자료 본문 (정적 파일. DB 아님 — 변하지 않고 오프라인 열람이 필요)
- `scripts/` — 검증 하네스 + 자료 수집 스크립트

## 명령어

- **검증(전체)**: `npm test`
- 프론트 정적 검사: `node scripts/check-web.mjs`
- GAS 로직 단위 테스트(모의 SpreadsheetApp): `node scripts/test-gas-actions.mjs`
- 지표·독려 메시지 단위 테스트: `node scripts/test-stats.mjs`
- 참고자료 JSON 검증: `node scripts/test-library-data.mjs`
- 맥체인 데이터 검증: `node scripts/test-mccheyne.mjs`
- 참고자료 재수집: `node scripts/fetch-library.mjs`
- 로컬 미리보기: `node scripts/mock-server.mjs 8788` → http://localhost:8788

## 규칙 (에이전트·서브에이전트 공통 — 반드시 준수)

### 작업 범위 (하네스)

- **이 저장소(`D:\00 workspace\01_Project\학부모 경건생활 앱`) 밖의 파일을 수정하지 않는다.**
  참고 저장소(scratchpad의 `godly-ref`)는 **읽기 전용**이다.
- **요구사항 고정**: 위 "핵심 기능" 9개 목록에 없는 기능을 임의로 추가·삭제하지 않는다.
  변경이 필요하면 총괄 에이전트에게 **보고만** 한다.
- **API 계약 고정**: `docs/specs/2026-07-30-api-contract.md`의 스키마·액션 계약을 임의로 바꾸지 않는다.
  프론트·백엔드·테스트는 이 계약 문서를 단일 기준으로 삼는다.
- 리팩터링·성능 개선·디자인 변경을 요청 없이 착수하지 않는다.

### 완료 기준

- `npm test`가 통과하고, 위 요구사항 9개 목록과 **항목별로 대조 확인**한 뒤에만 완료를 선언한다.
- 검증 명령을 실행하지 않은 채로 "동작한다"고 주장하지 않는다.

### 보안 불변식 (절대 위반 금지)

1. 서버는 **검증된 ID 토큰의 email만** 신뢰한다. 클라이언트가 보낸 `email`·`role`·`구분`은 **무시**한다.
2. 조회 액션은 반드시 토큰 email로 행을 필터한다. 교사 전용 액션은 `isTeacher_` 통과를 먼저 확인한다.
3. **교사·관리자 권한은 `MEMBERS_교사` 시트를 사람이 직접 편집해야만 부여된다.**
   `register` 액션은 어떤 입력으로도 교사 권한을 만들 수 없다.
4. 기도제목(`PRAYERS`)은 본인 + 교사만 조회 가능하다. 다른 학생·학부모에게 노출되는 경로를 만들지 않는다.
5. 시트에 쓰는 모든 자유 텍스트에 `sanitizeCell_`(수식 인젝션 방어)를 적용한다.
6. 오류 응답에 스택 트레이스·시트 ID·내부 경로를 담지 않는다.

### 비밀값

- 이 앱에 진짜 비밀값은 없다: OAuth Client ID와 GAS URL은 프론트에 공개되는 값이고,
  시트 ID는 알아도 접근 권한이 없으면 열람 불가 (참고 저장소 2026-07-19 결정 계승).
- 단, 토큰·비밀키·`.env`류·`REGISTER_CODE` 실제 값은 **커밋 금지**.
- `REGISTER_CODE`는 Apps Script의 스크립트 속성에만 넣는다.

### 사용자 대상

- 저장소 소유자는 **비개발자**다. `docs/ops/` 문서는 클릭 순서 수준으로 구체적으로 쓴다.
- 커밋 메시지·문서·UI 문구는 한국어로 작성한다.

## Git 커밋 규칙

변경 사항은 **의미 단위로 나누어** 커밋한다. 한 커밋에 무관한 파일을 섞지 않는다.
